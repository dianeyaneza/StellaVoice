# LiveKit agent worker process
# Use this if you don't need an HTTP server.
web: python agent.py

# If you add FastAPI health endpoints in future, switch to:
# web: uvicorn api:app --host 0.0.0.0 --port $PORT
